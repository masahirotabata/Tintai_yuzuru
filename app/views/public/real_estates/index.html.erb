  <th>譲れる物件一覧</th>
      <th>譲れる物件一覧が表示されます。</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody.tab>
    
    <% @real_estates.each do |real_estate| %>
    
        <td><%= link_to real_estate.real_estate_name, public_real_estate_path(real_estate), class: "real_estate_#{real_estate.id}" %></td>
        <td><%= real_estate.nearest_station %></td>
        <td><%= real_estate.structure %></td>
        
       
<%= link_to('賃貸物件詳細', public_real_estate_path(real_estate.id)) %>

  </tbody>
</table>

<%= link_to "物件投稿者",admin_customer_path(real_estate.customer_id) %>

<h1>物件投稿者をフォローしてマッチングすると、上記物件を交渉することができます。</h1>
  <% if real_estate.customers %>
        フォロー数: <%=link_to(@customer.followings.count,public_customer_relationships_path(real_estate.customer_id)) %>
        フォロワー数: <%= link_to(@customer.followers.count,public_customer_relationship_path(real_estate.customer_id))  %>
  <% if current_customer.following?(@customer) %>
  <%= link_to "フォロー外す", admin_customer_relationship_path(current_customer), method: :delete %>
  <% else %>
  <%= link_to "フォローする", admin_customer_relationships_path(current_customer), method: :post %>
  <% end %>
  <% end %>
  
  <% if real_estate.customers.matchers %>

    <div class='col-md-9 offset-md-1'>
      <div class='row'>
        <div class='col'>
          <%= attachment_image_tag real_estate, :image, :fill, 200, 200, fallback: "no-image.jpg" %>
        </div>
        <div class='col'>
          <p class='cake_name'><%= real_estate.real_estate_name %></p>
          <p class='cake_introduction'><%= simple_format( real_estate.detail ) %></p>
          <p>提供価格:¥<%= real_estate.add_tax_price.to_s(:delimited) %>(税込)</p>
          <%= form_with model: @order_real_estate, url: public_order_real_estates_path, method: :post do |f| %>
            <!--顧客情報送信フィールド-->
            <%= f.hidden_field :real_estate_id, value: real_estate.id %>
            <%= f.hidden_field :customer_id, value: current_customer.id %>
            <!--購入数のセレクトボックス-->
            <%= f.select :pieces, options_for_select((1..10).to_a) %>
            <%= f.submit "交渉物件に加える" %>
        </div>
         <% end %>
      </div>
    </div>
  </div>
</div>
<% else %>
 <% end %>
<% end %>
