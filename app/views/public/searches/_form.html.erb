<% if customer_signed_in? %>
  <div class="search_form">111
    <%= form_with url: public_searches_path, local: true, method: :get do |f| %>
      <%#= f.text_field :word %>
      <%#= f.select :range,  Area.real_estate_ereas.keys, {class: 'Areas'} %>
      <%#= f.select :range,  options_for_select(Area.real_estate_ereas.map{|k,v|[k,v]}), {class: 'Areas'} %>
      <%= f.select :range,  options_for_select(RealEstate.area_ids.map{|k,v|[k,v]}), {class: 'Areas'} %>
      <%#= f.select :search, options_for_select([["完全一致","perfect_match"], ["前方一致","forward_match"],  ["後方一致","backword_match"], ["部分一致","partial_match"]]) %>
      <%= f.submit "検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>
